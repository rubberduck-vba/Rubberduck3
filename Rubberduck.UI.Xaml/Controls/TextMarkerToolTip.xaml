<ToolTip x:Class="Rubberduck.UI.Xaml.Controls.TextMarkerToolTip"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Rubberduck.UI.Xaml.Controls"
             mc:Ignorable="d" 
             Padding="0" 
             d:DesignHeight="24" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <!-- TODO get colors & font sizes from config -->
        <TextBlock Grid.Row="0" Margin="5,5,5,0" Padding="1,1,1,0" FontSize="12" FontWeight="Bold" Text="{Binding TipTitle}" />
        <TextBlock Grid.Row="1" Margin="5,0,5,5" Padding="1,0,1,1" FontSize="10" Text="{Binding TipText}" />
    </Grid>
</ToolTip>
