<Window x:Class="Rubberduck.Editor.Message.MessageWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Rubberduck.Editor.Message" 
        xmlns:mdxaml="clr-namespace:MdXaml;assembly=MdXaml"
        mc:Ignorable="d"
        Title="Rubberduck" WindowStyle="SingleBorderWindow"
        Height="200" MinHeight="200" MaxHeight="500"
        Width="512" MinWidth="400" MaxWidth="800"
        ResizeMode="CanResizeWithGrip" 
        SizeToContent="WidthAndHeight" SnapsToDevicePixels="True">
    <Window.Resources>
        <ResourceDictionary>
            <BitmapImage x:Key="BackgroundDucky" UriSource="pack://application:,,,/Rubberduck.Editor;component/Resources/Custom/background-ducky.png" />
            <BitmapImage x:Key="ErrorIcon" UriSource="pack://application:,,,/Rubberduck.Editor;component/Resources/FugueIcons/cross-circle.png" />
            <BitmapImage x:Key="WarnIcon" UriSource="pack://application:,,,/Rubberduck.Editor;component/Resources/FugueIcons/exclamation-diamond.png" />
            <BitmapImage x:Key="InfoIcon" UriSource="pack://application:,,,/Rubberduck.Editor;component/Resources/FugueIcons/information.png" />
            <Style x:Key="ActionButtonStyle" TargetType="Button">
                <Style.Setters>
                    <Setter Property="MinWidth" Value="92" />
                    <Setter Property="Height" Value="24" />
                    <Setter Property="Margin" Value="1,0" />
                </Style.Setters>
            </Style>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Canvas Grid.RowSpan="4" HorizontalAlignment="Right"
                Width="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Window}}, Path=ActualWidth}"
                Height="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Window}}, Path=ActualHeight}">
            <Image Source="{DynamicResource BackgroundDucky}" Stretch="None">
                <Image.RenderTransform>
                    <TransformGroup>
                        <TranslateTransform X="-384" />
                    </TransformGroup>
                </Image.RenderTransform>
            </Image>
        </Canvas>

        <Label Grid.Row="0" Content="Title goes here." FontWeight="Bold" FontSize="16" Margin="10,0" />

        <mdxaml:MarkdownScrollViewer Grid.Row="1" Margin="4" Padding="16,0" MinHeight="100" Height="400"
                                     VerticalAlignment="Top"
                                     VerticalScrollBarVisibility="Auto" xml:space="preserve">
This is actually a ***markdown** document*.

        </mdxaml:MarkdownScrollViewer>

        <Expander Grid.Row="2" Header="More information" Margin="4">
            <Expander.HeaderTemplate>
                <DataTemplate>
                    <Label FontSize="11" Content="More information" ToolTip="Expand/collapse to show/hide details" />
                </DataTemplate>
            </Expander.HeaderTemplate>
            <mdxaml:MarkdownScrollViewer Grid.Row="1" Margin="4" Padding="16,0" Height="102"
                                     VerticalScrollBarVisibility="Auto" xml:space="preserve">
We ❤️ Markdown!
```vb
Option Explicit
'we can also have code blocks!
```
Hopefully we'll figure out how to tweak the fonts.

        </mdxaml:MarkdownScrollViewer>
        </Expander>

        <Border Grid.Row="2" Background="Black" Opacity="0.05" Grid.RowSpan="2" />

        <StackPanel Grid.Row="3" Margin="16,6" Orientation="Horizontal" HorizontalAlignment="Right">
            <ItemsControl ItemsSource="{Binding Actions}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Content="{Binding Title}" IsDefault="{Binding IsDefaultAction}" Style="{StaticResource ActionButtonStyle}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>

    </Grid>
</Window>
